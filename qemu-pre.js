
//add some undefined vars, come up through emscripten compilation.
if(_get_human_readable_size_suffixes === undefined) 
    var _get_human_readable_size_suffixes = '';

if(_vmdk_create_desc_template === undefined)
    var _vmdk_create_desc_template = '';

